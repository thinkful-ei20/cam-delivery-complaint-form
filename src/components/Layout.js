import React from 'react';
import Form from './Form';

export default class Layout extends React.Component {
  render() {
    return (
      <div>
        <h1>Layout</h1>
        <Form />
      </div>
    );
  }
}